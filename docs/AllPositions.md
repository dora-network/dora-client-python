# AllPositions


## Properties

Name | Type | Description | Notes
------------ | ------------- | ------------- | -------------
**all_positions** | **Dict[str, Dict[str, Dict[str, Position]]]** |  | 

## Example

```python
from dora_client.models.all_positions import AllPositions

# TODO update the JSON string below
json = "{}"
# create an instance of AllPositions from a JSON string
all_positions_instance = AllPositions.from_json(json)
# print the JSON string representation of the object
print(AllPositions.to_json())

# convert the object into a dict
all_positions_dict = all_positions_instance.to_dict()
# create an instance of AllPositions from a dict
all_positions_from_dict = AllPositions.from_dict(all_positions_dict)
```
[[Back to Model list]](../README.md#documentation-for-models) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to README]](../README.md)


